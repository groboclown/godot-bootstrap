

config = {
    bootstrap: "boot",
    components: [
		error_codes, error_dialog, ext_controllers,
		button_container, unit_tests, title_button_theme, long_text,
        select_list
	],
    dirmap: {
        lib: ".",
        tests: "../tests"
    }
}
